# 🎟️ React 좌석 선택 페이지 — Round 11 (좌석맵 완전 중앙 정렬판)

> **목표:** 좌석맵과 사이드바의 시각적 중심선 완전 정렬 (멜론티켓형 레이아웃 완성)

---

## 🎯 현재 상태 (Round 10 이후)

| 항목 | 상태 | 비고 |
|------|------|------|
| 좌석맵 정렬 | 살짝 왼쪽으로 치우침 | min-width 부족 |
| 사이드바 | 완벽히 정렬 | 문제 없음 |
| 텍스트 여백 | 안정적 | padding-left 적용 |
| 전체 중앙선 | 시각적 균형 미세하게 틀어짐 | 좌석맵 비율 조정 필요 |

---

## ✅ 개선 핵심 포인트

> 좌석맵의 실제 시각적 중심선을 화면의 중앙선에 정확히 맞추기 위해  
> 최소 폭(`min-width`)과 마진(`margin-left`)을 조정합니다.

---

## 🧩 1️⃣ 페이지 Wrapper 구조

```css
.page-wrapper {
  display: flex;
  justify-content: center;
  align-items: flex-start;
  width: 100%;
}
```

📌 화면 전체를 강제로 중앙 정렬하여, grid 컨테이너가 항상 중앙에 위치.

---

## 🧱 2️⃣ Grid 레이아웃 (좌우 비율 재정의)

```css
.seat-container {
  display: grid;
  grid-template-columns: 5fr 1.3fr; /* 좌석맵 : 사이드바 비율 */
  gap: 60px;
  max-width: 1600px;
  width: 100%;
  padding: 40px 0;
  justify-content: center;
}
```

📌 전체 비율을 5:1.3으로 조정해 좌석맵 비중을 늘리고 중앙 밸런스를 강화.

---

## 🎨 3️⃣ 좌석맵 중심 보정

```css
.seat-map {
  background: #fff;
  border-radius: 12px;
  border: 1px solid #e5e7eb;
  padding: 40px;
  height: 640px;
  min-width: 1050px; /* 좌석맵 중심 정렬 핵심 */
  margin-left: 60px; /* 시각적 균형 보정 */
  box-shadow: 0 2px 8px rgba(0,0,0,0.05);
}
```

📌 좌석맵 자체가 비어 있을 때도 중앙선 기준이 유지되도록 최소 폭 지정.  
마진을 살짝 주어 실제 시각 중심선을 맞춤.

---

## 🖌️ 4️⃣ 사이드바 보정

```css
.seat-sidebar {
  padding-left: 10px;
}
```

📌 `"좌석색상안내"` 텍스트가 벽에 붙지 않게끔 미세 여백 확보.

---

## 📱 5️⃣ 반응형 유지 (모바일 대응)

```css
@media (max-width: 768px) {
  .seat-container {
    grid-template-columns: 1fr;
    gap: 24px;
    padding: 20px;
  }
  .seat-map {
    min-width: auto;
    height: 500px;
    margin-left: 0;
  }
  .seat-sidebar {
    order: -1; /* 상단으로 이동 */
  }
}
```

📌 작은 화면에서는 자동으로 1열로 정렬, 좌석맵 폭도 자동으로 조정.

---

## ✅ 최종 구조 요약

| 구역 | 역할 |
|------|------|
| `.page-wrapper` | 전체 화면 중앙 정렬 |
| `.seat-container` | 좌석맵/사이드바 균형 유지 |
| `.seat-map` | 중심선 + 최소 폭 확보 |
| `.seat-sidebar` | 여백 안정화 |
| 반응형 | 자동 전환 |

---

## 💡 추가 제안

- `"무대(STAGE)"` 영역을 고정 배치하여 사용자가 중심을 인식하기 쉽게 만듦  
- 선택된 좌석은 `transform: scale(1.05)` 효과로 강조  
- 좌석 Hover 시 Tooltip으로 상세 정보 표시 (`A열 3번 — 12,000원`)

---

**📅 작성일:** 2025-10-31  
**🧩 버전:** React UI Feedback Round 11  
**✍ 작성자:** ChatGPT (GPT-5)
